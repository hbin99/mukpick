<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jumpstd.mukpick.admin.mapper.MemberMapper">
    <select id="memberAllList" resultType="">
        SELECT USER_ID
             , PASSWORD
             , USER_NAME
             , PHONE
             , EMAIL
             , ROLE_TYPE
             , GENDER
             , AGE
             , REGISTER_DATE
        FROM TB_USER
        ORDER BY REGISTER_DATE;
    </select>
</mapper>
